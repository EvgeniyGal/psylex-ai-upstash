/** Minutes before scheduled start when Start Mediation becomes available. */
export const START_BUTTON_LEAD_MINUTES = 10;

export const START_BUTTON_LEAD_MS = START_BUTTON_LEAD_MINUTES * 60_000;

/** Delay before sending the mediator to room details after the session ends. */
export const SESSION_END_REDIRECT_MS = 2500;
